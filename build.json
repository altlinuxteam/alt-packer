{
    "variables": {
        "server_iso_url": "",
        "server_iso_checksum": "",
        "server_iso_checksum_type": "",

        "ssh_user": "",
        "ssh_pass": "",

        "vagrant_user": "",
        "vagrant_pass": "",

        "http_port_min": "",
        "http_port_max": ""
    },

    "builders": [
        {
            "name": "srv.qemu",
            "type": "qemu",
            "accelerator": "kvm",
            "vm_name": "alt-server-p8",
            "disk_size": 30000,
            "disk_cache": "unsafe",
            "output_directory": "output-qemu",
            "headless": true,
            "qemuargs": [
                [ "-m", "2048M" ]
            ],

            "iso_url": "{{user `server_iso_url`}}",
            "iso_checksum": "{{user `server_iso_checksum`}}",
            "iso_checksum_type": "{{user `server_iso_checksum_type`}}",

            "http_port_min": "{{user `http_port_min`}}",
            "http_port_max": "{{user `http_port_max`}}",

            "ssh_username": "{{ user `ssh_user` }}",
            "ssh_password": "{{ user `ssh_pass` }}",
            "ssh_wait_timeout": "60m",
            "ssh_pty": true,

            "http_directory": "./",

            "boot_wait": "2s",
            "boot_command": [
                "<esc><wait><enter><wait>",
                "alt0/vmlinuz initrd=alt0/full.cz changedisk ramdisk_size=239301 lang=ru_RU noeject quiet=1 showopts net.ifnames=0 automatic=method:http,network:dhcp,server:{{.HTTPIP}},directory:/distr ai curl=http://{{.HTTPIP}}:{{.HTTPPort}}/metadata/srv/kvm/",
                "<wait><enter>"
            ],

            "shutdown_command": "sudo shutdown -P now"
        },

        {
            "name": "srv.vbox",
            "type": "virtualbox-iso",
            "guest_os_type": "Linux_64",
            "vm_name": "alt-server-p8",
            "disk_size": 30000,
            "output_directory": "output-vbox",
            "headless": true,
            "keep_registered": false,
            "guest_additions_mode": "disable",
            "vboxmanage": [
                ["modifyvm", "{{.Name}}", "--memory", "2048"],
                ["modifyvm", "{{.Name}}", "--cpus", "1"]
            ],

            "iso_url": "http://mirror.yandex.ru/altlinux/p8/images/server/alt-8-server-x86_64.iso",
            "iso_checksum": "719d43ed40dc5774c8e40166746856d5b94e41c5",
            "iso_checksum_type": "sha1",

            "http_port_min": "{{user `http_port_min`}}",
            "http_port_max": "{{user `http_port_max`}}",

            "ssh_username": "{{ user `vagrant_user` }}",
            "ssh_password": "{{ user `vagrant_pass` }}",
            "ssh_wait_timeout": "60m",
            "ssh_pty": true,

            "http_directory": "./",

            "boot_wait": "5s",
            "boot_command": [
                "<esc><wait><enter><wait>",
                "alt0/vmlinuz initrd=alt0/full.cz changedisk ramdisk_size=239301 lang=ru_RU noeject quiet=1 showopts net.ifnames=0 automatic=method:http,network:dhcp,server:{{.HTTPIP}},directory:/distr ai curl=http://{{.HTTPIP}}:{{.HTTPPort}}/metadata/srv/vbox/",
                "<wait><enter>"
            ],

            "shutdown_command": "sudo shutdown -P now",
            "post_shutdown_delay": "10s",

            "export_opts":
            [
                "--manifest",
                "--vsys", "0",
                "--description", "ALT Server P8 x86_64",
                "--version", "p8"
            ],

            "format": "ova"
        }
    ],

    "provisioners": [
        {
            "type": "shell",
            "script": "scripts/fix_sudoers.sh",
            "environment_vars": [
                "PASS='{{user `ssh_pass`}}'"
            ]
        },
        {
            "type": "file",
            "source": "scripts/",
            "destination": "/tmp/"
        },
        {
            "type": "shell",
            "execute_command": "sudo sh -c '{{ .Vars }} {{ .Path }}'",
            "inline": [
                "mv /tmp/rc.apt /etc/rc.d/scripts/apt-cache-on-tmp",
                "mv /tmp/rc.local /etc/rc.d/rc.local",
                "mv /tmp/tmp.cache.conf /etc/apt/apt.conf.d/tmp.cache.conf",
                "/etc/rc.d/scripts/apt-cache-on-tmp",
                "apt-get -y update",
                "apt-get -y dist-upgrade"
            ]
        },
        {
            "type": "shell",
            "inline": [
                "mkdir -p ${HOME}/.ssh",
                "curl http://${PACKER_HTTP_ADDR}/pubkeys/vagrant.pub -o ${HOME}/.ssh/authorized_keys"
            ]
        },
        {
            "type": "shell",
            "script": "scripts/install-guest-tools.sh",
            "environment_vars": [
                "SSH_USER='{{user `ssh_user`}}'"
            ],
            "only": ["srv.vbox"]
        },
        {
            "type": "shell",
            "inline": "sudo rm -f /etc/apt/sources.list.d/sources.installer.list"
        }
    ],
    "post-processors": [
        {
            "type": "checksum",
            "checksum_types": [ "md5", "sha1" ],
            "output": "results/{{.BuildName}}.{{.ChecksumType}}"
        },
        {
            "type": "vagrant",
            "only": [ "srv.vbox" ],
            "output": "results/{{.BuildName}}.box"
        },
        {
            "type": "compress",
            "output": "results/{{.BuildName}}.tar.gz",
            "only": [ "srv.qemu" ]
        },
        {
            "type": "manifest",
            "output": "results/manifest.json",
            "strip_path": true
        }
    ]
}
